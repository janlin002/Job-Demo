import React from 'react';
import { Styled{{pascalCase name}} } from './{{pascalCase name}}.style';

export const {{pascalCase name}} = ({ className }) => {
return (
<Styled{{pascalCase name}} className={className} data-testid="{{pascalCase name}}">
  {/* Code Here */}
</Styled{{pascalCase name}}>
);
};