import React from 'react'
// import { Jwt } from 'jsonwebtoken'

const SECRET = 'thisismynewproject'

const Index = () => {
  // const token = Jwt.sign({ foo: 'bar' }, SECRET, { expiresIn: '1 day' })
  // console.log(token, 'token')

  return (
    <div>Index</div>
  )
}

export default Index